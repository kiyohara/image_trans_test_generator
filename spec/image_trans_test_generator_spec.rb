require 'spec_helper'

describe ImageTransTestGenerator do
  it 'has a version number' do
    expect(ImageTransTestGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
